{
  "entities": {
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "Represents a basic raw good or inventory item used to prepare menu items.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the product (e.g., 'Coffee Beans', 'Whole Milk')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the product."
        },
        "unit": {
          "type": "string",
          "enum": ["kg", "liters", "units"],
          "description": "The standard unit of measurement for the product."
        }
      },
      "required": ["name", "unit"]
    },
    "CentralStockItem": {
      "title": "Central Stock Item",
      "type": "object",
      "description": "Represents a product's stock level in the central warehouse.",
      "properties": {
        "productId": {
          "type": "string",
          "description": "Reference to the Product ID."
        },
        "quantity": {
          "type": "number",
          "description": "The total quantity available in the central warehouse."
        }
      },
      "required": ["productId", "quantity"]
    },
    "LocalStockItem": {
      "title": "Local Stock Item",
      "type": "object",
      "description": "Represents a product's stock level in a specific location's inventory.",
      "properties": {
        "productId": {
          "type": "string",
          "description": "Reference to the Product ID."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity available at the local inventory."
        }
      },
      "required": ["productId", "quantity"]
    },
    "RecipeIngredient": {
      "title": "Recipe Ingredient",
      "type": "object",
      "description": "A single ingredient and its quantity required for a menu item.",
      "properties": {
        "productId": {
          "type": "string",
          "description": "Reference to the Product ID."
        },
        "quantity": {
          "type": "number",
          "description": "The amount of this product required for the recipe."
        }
      },
      "required": ["productId", "quantity"]
    },
    "MenuItem": {
      "title": "Menu Item",
      "type": "object",
      "description": "Represents a single item on the restaurant's menu, including its recipe.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the menu item (e.g., 'Cheeseburger')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the menu item."
        },
        "price": {
          "type": "number",
          "description": "The price of the menu item."
        },
        "category": {
          "type": "string",
          "enum": ["Appetizer", "Main Course", "Dessert", "Beverage"],
          "description": "The category the menu item belongs to."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the image for the menu item."
        },
        "isAvailable": {
          "type": "boolean",
          "description": "Whether the menu item is currently available to be ordered."
        },
        "recipe": {
          "type": "array",
          "items": {
            "$ref": "#/entities/RecipeIngredient"
          },
          "description": "The list of ingredients and quantities required to make this item."
        }
      },
      "required": ["name", "price", "category", "isAvailable"]
    },
    "Order": {
      "title": "Order",
      "type": "object",
      "description": "Represents a customer's order for a specific table at a specific location.",
      "properties": {
        "locationId": {
          "type": "string",
          "description": "The ID of the location where the order was placed."
        },
        "tableNumber": {
          "type": "number",
          "description": "The table number for the order."
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/entities/OrderItem"
          },
          "description": "The list of menu items included in the order."
        },
        "status": {
          "type": "string",
          "enum": ["Pending", "Preparing", "Ready", "Served", "Paid"],
          "description": "The current status of the order."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the order was created."
        }
      },
      "required": ["locationId", "tableNumber", "items", "status", "createdAt"]
    },
    "OrderItem": {
      "title": "Order Item",
      "type": "object",
      "description": "Represents a single menu item within an order.",
      "properties": {
        "menuItemId": {
          "type": "string",
          "description": "Reference to the MenuItem ID."
        },
        "name": {
          "type": "string"
        },
        "quantity": {
          "type": "number"
        },
        "price": {
          "type": "number"
        }
      },
      "required": ["menuItemId", "name", "quantity", "price"]
    },
    "Location": {
      "title": "Location",
      "type": "object",
      "description": "Represents a single business location, like a coffee shop.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The display name of the location (e.g., 'Cafetería Avellaneda')."
        },
        "address": {
          "type": "string",
          "description": "The physical address of the location."
        },
        "isActive": {
          "type": "boolean",
          "description": "Whether the location is currently operational."
        }
      },
      "required": ["name", "isActive"]
    },
    "StockTransfer": {
      "title": "Stock Transfer",
      "type": "object",
      "description": "Represents a transfer of stock from the central warehouse to a local inventory.",
      "properties": {
        "locationId": {
          "type": "string",
          "description": "The destination location ID."
        },
        "productId": {
          "type": "string",
          "description": "The ID of the product being transferred."
        },
        "quantity": {
          "type": "number",
          "description": "The amount of product transferred."
        },
        "status": {
          "type": "string",
          "enum": ["Requested", "Approved", "Shipped", "Received"],
          "description": "The status of the stock transfer."
        },
        "requestedAt": {
          "type": "string",
          "format": "date-time"
        },
        "approvedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["locationId", "productId", "quantity", "status", "requestedAt"]
    }
  },
  "auth": {
    "providers": ["anonymous"]
  },
  "firestore": {
    "/products/{productId}": {
      "schema": { "$ref": "#/entities/Product" },
      "description": "Master list of all raw goods and products."
    },
    "/menuItems/{menuItemId}": {
      "schema": { "$ref": "#/entities/MenuItem" },
      "description": "Master list of all menu items offered, including their recipes."
    },
    "/locations/{locationId}": {
      "schema": { "$ref": "#/entities/Location" },
      "description": "List of all business locations."
    },
    "/centralStock/{productId}": {
      "schema": { "$ref": "#/entities/CentralStockItem" },
      "description": "Stock levels of products in the central warehouse."
    },
    "/locations/{locationId}/localStock/{productId}": {
      "schema": { "$ref": "#/entities/LocalStockItem" },
      "description": "Stock levels for a specific product at a specific location."
    },
    "/orders/{orderId}": {
      "schema": { "$ref": "#/entities/Order" },
      "description": "Stores all customer orders from all locations."
    },
    "/stockTransfers/{transferId}": {
      "schema": { "$ref": "#/entities/StockTransfer" },
      "description": "History of all stock transfers from central to local inventories."
    }
  }
}
