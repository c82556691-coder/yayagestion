rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to products for development purposes.
    match /products/{productId} {
      allow read, write;
    }

    // Lock down customer data for now.
    match /customers/{customerId} {
      allow read, write: if false;
    }
  }
}
